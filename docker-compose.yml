version: "3.7"

services:
  redis:
    image: redis
    ports:
      - "6379:6379"

  server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    depends_on:
      - redis
    command: bash -c "./target/debug/protego"
